# Dependencies
#FIND_PACKAGE(Eigen3 3.3 REQUIRED NO_MODULE)
#INCLUDE_DIRECTORIES(${EIGEN3_INCLUDE_DIR})
#SET(DEPS Eigen3::Eigen)

FILE(GLOB_RECURSE ALL_LIB_CPP *.cpp)
ADD_LIBRARY(lib
  ${ALL_LIB_CPP}
)
TARGET_LINK_LIBRARIES(lib
  ${DEPS}
)
